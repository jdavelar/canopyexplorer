% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/efa_functions.R
\name{tag_correlations}
\alias{tag_correlations}
\title{Correlation of Tag Variables}
\usage{
tag_correlations(
  data,
  prefix = "practices_",
  output = c("dataframe", "table", "plot"),
  use = "pairwise.complete.obs",
  method = "pearson"
)
}
\arguments{
\item{data}{A data frame containing tag variables.}

\item{prefix}{A string specifying the prefix of tag columns (default is \code{"practices_"}).}

\item{output}{A string specifying the desired output: \code{"dataframe"}, \code{"table"}, or \code{"plot"}.}

\item{use}{A string specifying how to handle missing values. Default is \code{"pairwise.complete.obs"}.}

\item{method}{A string specifying the correlation method (\code{"pearson"}, \code{"spearman"}, \code{"kendall"}). Default is \code{"pearson"}.}
}
\value{
A correlation matrix (data frame) or a table/plot (if specified).
}
\description{
Generates a correlation matrix for tag variables in a dataset.
}
\examples{
test_data <- data.frame(
  practices_a = c(1, 0, 1, 1, 0),
  practices_b = c(0, 1, 1, 0, 1),
  practices_c = c(1, 1, 0, 1, 1)
)

# Return correlation dataframe
tag_correlations(test_data)

# Display as table
tag_correlations(test_data, output = "table")

# Display as plot
tag_correlations(test_data, output = "plot")

}
