% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/label_tags.R
\name{label_tags}
\alias{label_tags}
\title{Label Tags}
\usage{
label_tags(
  x,
  label_data = NULL,
  capitalize = "none",
  wrap = Inf,
  return_type = c("vector", "named")
)
}
\arguments{
\item{x}{A character vector of variable names to convert.}

\item{label_data}{A data frame with \code{tag} and \code{label} columns.
If \code{NULL}, the function will use the default data stored in the package.}

\item{capitalize}{A string specifying how to capitalize labels.
Options are \code{"none"}, \code{"first"}, \code{"title"}. Default is \code{"none"}.}

\item{wrap}{An integer specifying the width at which to wrap text.
Default is \code{Inf} (no wrapping).}

\item{return_type}{A string specifying the output format.
Options are \code{"vector"} (default) or \code{"named"}.}
}
\value{
A character vector or named vector of converted labels.
}
\description{
A helper function to convert tag variable names into labels. Can be used for
ggplot axis labels, tables, or display.
}
\examples{
# Example 1: Direct conversion
label_tags(c("practices_a_la_carte", "adaptive_content"))

# Example 2: Rename ggplot axis labels
ggplot(mpg, aes(class, hwy)) +
  geom_col() +
  scale_x_discrete(labels = label_tags)

# Example 3: Rename dataframe columns
df <- data.frame(a = 1:5, b = 6:10)
names(df) <- label_tags(names(df), return_type = "named")

}
