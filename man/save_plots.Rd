% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/save_plots.R
\name{save_plots}
\alias{save_plots}
\title{Save ggplot objects for CRPE Branding}
\usage{
save_plots(
  plot,
  file,
  dir = NULL,
  fig_width = 9,
  fig_height = 7,
  formats = c("png", "svg", "eps")
)
}
\arguments{
\item{plot}{A ggplot object to save.}

\item{file}{A character string specifying the desired file name (without extension).}

\item{dir}{A character string specifying the directory to save the file in.
If \code{NULL}, the function will default to the current working directory.}

\item{fig_width}{A numeric value specifying the width of the plot in inches. Default is 9.}

\item{fig_height}{A numeric value specifying the height of the plot in inches. Default is 7.}

\item{formats}{A character vector specifying the file format(s) to save.
Options are \code{"png"}, \code{"svg"}, and \code{"eps"}. Default is \code{c("png", "svg", "eps")}.}
}
\description{
This wrapper function saves a ggplot object as either (1) a PNG file,
(2) an SVG file, or (3) an EPS file. The function creates subdirectories
for each format automatically if they do not already exist.
}
\details{
\itemize{
\item PNG files are saved at 96 dpi for consistent on-screen and print quality.
\item Subdirectories are automatically created within \code{dir} for each file format.
\item The function will attempt to save each file type separately and will return
a warning if a format fails to save.
}
}
\examples{
library(ggplot2)
plot <- ggplot(mpg, aes(x = class, fill = drv)) +
  geom_bar() +
  theme_minimal()

# Save as PNG and SVG
save_plots(plot, file = "example_plot", formats = c("png", "svg"))

}
